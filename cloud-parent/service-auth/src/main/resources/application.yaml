server:
  port: 46200
  servlet:
    context-path: /auth
spring:
  application:
    name: service-auth
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:45002/cloud_server_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: local1234
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
