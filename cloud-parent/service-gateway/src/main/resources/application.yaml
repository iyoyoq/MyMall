server:
  port: 46100
spring:
  application:
    name: service-gateway
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
    gateway:
      routes:
#        - id: item # 路由规则id，自定义，唯一
#          uri: lb://item-service # 路由的目标服务，lb代表负载均衡，会从注册中心拉取服务列表
#          predicates: # 路由断言，判断当前请求是否符合当前规则，符合则路由到目标服务
#            - Path=/items/**,/search/** # 这里是以请求路径作为判断规则
#        - id: cart
#          uri: lb://cart-service
#          predicates:
#            - Path=/carts/**
        - id: auth
          uri: lb://service-auth
          predicates:
            - Path=/auth/**
#        - id: trade
#          uri: lb://trade-service
#          predicates:
#            - Path=/orders/**
#        - id: pay
#          uri: lb://pay-service
#          predicates:
#            - Path=/pay-orders/**
  redis:
    host: 127.0.0.1
    port: 45003
    password: local1234
    database: 0
mymall:
  filter-path:
#    token 免检路径
    login-check-exclude-path:
#      - /auth/**
      - /error/**
